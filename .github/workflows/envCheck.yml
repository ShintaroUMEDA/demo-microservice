name: env check

on:
  workflow_dispatch:

jobs:

  build:

    runs-on:  self-hosted

    steps:
    - name: list items
      run: |
        ls -al /var/run/
        ls -al /var/run/docker/
        sudo ln -s /var/run/docker/* /var/run/*
        ls -al /var/run/
